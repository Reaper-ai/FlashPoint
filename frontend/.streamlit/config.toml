[theme]
base="dark"

primaryColor="#00e5ff"       # Cyan (Main Accent)
backgroundColor="#0b0e11"    # Deep Gunmetal (Background)
secondaryBackgroundColor="#151921" # Slightly lighter panel
textColor="#e0e6ed"          # Off-white (easier on eyes than pure white)
font="sans serif"

[server]
headless = true